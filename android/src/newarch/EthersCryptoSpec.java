package com.etherscrypto;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class EthersCryptoSpec extends NativeEthersCryptoSpec {
  EthersCryptoSpec(ReactApplicationContext context) {
    super(context);
  }
}
